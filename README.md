# PREDOC Python Codespace Template

Open in a Codespace to get started writing Python code for data analysis.

Place Jupyter Notebooks in the `notebooks/` folder and datasets (e.g. CSVs) in the `data/` folder.

This notebook introduces linear regression (Ordinary Least Squares) in Python and focuses on interpreting linear models with numerical and categorical predictors.
